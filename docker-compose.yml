# tell docker what version of the docker-compose.yml we're using
version: '3'
# define the network
networks:
  web-network:
# start the services section
services:
  # define the name of our service
  # corresponds to the "--name" parameter
  docker-nginx:
    build: 
      context: ./nginx
    # defines the port mapping
    # corresponds to the "-p" flag
    ports:
      - "80:80"
    tty: true
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ../web:/var/www
      - ./nginx/conf.d:/etc/nginx/conf.d
    networks:
      - web-network
  # PHP 7.4 專用 FPM 
  docker-php-fpm:
    build: 
      context: ./php-fpm
    tty: true
    # 添加 ports 映射
    ports:
      - "6001:6001" 
    volumes:
      - ../web:/var/www
      - ./php-fpm/openssl.cnf:/etc/ssl/openssl.cnf
      - ./php-fpm/php.ini:/usr/local/etc/php/php.ini
    networks:
      - web-network
  # PHP 8.3.12 專用 FPM（與 docker-php-fpm 並行；Nginx 依站台改 fastcgi_pass）
  docker-php8-fpm:
    build:
      context: .
      dockerfile: php8-fpm/Dockerfile
    tty: true
    ports:
      # 主機連除錯埠分開避免與 php-fpm 的 6001 衝突
      - "6002:6001"
    volumes:
      - ../web:/var/www
      - ./php8-fpm/openssl.cnf:/etc/ssl/openssl.cnf
      - ./php8-fpm/php.ini:/usr/local/etc/php/php.ini
    networks:
      - web-network
  docker-mysql:
    build: 
      context: ./mysql
    volumes:
      - ~/mysql-docker-data:/var/lib/mysql
    ports:
      - 3306:3306
    environment:
      TZ: Asia/Taipei
      MYSQL_ROOT_PASSWORD: Aa123456
      MYSQL_DATABASE: mydb
    command: ["mysqld", "--default-authentication-plugin=mysql_native_password"]
  redis:
    image: redis:5.0.5-alpine
    container_name: "redis"
    ports:
      - "6379:6379"
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf
    networks:
      - web-network
    